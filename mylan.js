class Elem {
    constructor(config) {
        this.element = config.element;
        this.id = config.id;
        this.class = config.class;
        this.innerText = config.innerText;
        this.innerHTML = config.innerHTML;
        this.src = config.src;
        this.type = config.type;
        this.href = config.href;
        this.rel = config.rel;
        this.accept = config.accept;
        this.acceptCharset = config.acceptCharset;
        this.accesskey = config.accesskey;
        this.action = config.action;
        this.alt = config.alt;
        this.async = config.async;
        this.autocomplete = config.autocomplete;
        this.autofocus = config.autofocus;
        this.charset = config.charset;
        this.checked = config.checked;
        this.cite = config.cite;
        this.cols = config.cols;
        this.colspan = config.colspan;
        this.content = config.content;
        this.contenteditable = config.contenteditable;
        this.controls = config.controls;
        this.coords = config.coords;
        this.data = config.data;
        this.datetime = config.datetime;
        this.default = config.default;
        this.defer = config.defer;
        this.dir = config.dir;
        this.dirName = config.dirName;
        this.disabled = config.disabled;
        this.download = config.download;
        this.draggable = config.draggable;
        this.enctype = config.enctype;
        this.for = config.for;
        this.form = config.form;
        this.formaction = config.formaction;
        this.headers = config.headers;
        this.height = config.height;
        this.hidden = config.hidden;
        this.high = config.high;
        this.hreflang = config.hreflang;
        this.httpEquiv = config.httpEquiv;
        this.ismap = config.ismap;
        this.kind = config.kind;
        this.label = config.label;
        this.lang = config.lang;
        this.list = config.list;
        this.loop = config.loop;
        this.low = config.low;
        this.max = config.max;
        this.maxlength = config.maxlength;
        this.media = config.media;
        this.method = config.method;
        this.min = config.min;
        this.multiple = config.multiple;
        this.muted = config.muted;
        this.name = config.name;
        this.novalidate = config.novalidate;
        this.onabort = config.onabort;
        this.onafterprint = config.onafterprint;
        this.onbeforeprint = config.onbeforeprint;
        this.onbeforeunload = config.onbeforeunload;
        this.onblur = config.onblur;
        this.oncanplay = config.oncanplay;
        this.oncanplaythrough = config.oncanplaythrough;
        this.onchange = config.onchange;
        this.onclick = config.onclick;
        this.oncontextmenu = config.oncontextmenu;
        this.oncopy = config.oncopy;
        this.oncuechange = config.oncuechange;
        this.oncut = config.oncut;
        this.ondblclick = config.ondblclick;
        this.ondrag = config.ondrag;
        this.ondragend = config.ondragend;
        this.ondragenter = config.ondragenter;
        this.ondragleave = config.ondragleave;
        this.ondragover = config.ondragover;
        this.ondragstart = config.ondragstart;
        this.ondrop = config.ondrop;
        this.ondurationchange = config.ondurationchange;
        this.onemptied = config.onemptied;
        this.onended = config.onended;
        this.onerror = config.onerror;
        this.onfocus = config.onfocus;
        this.onhashchange = config.onhashchange;
        this.oninput = config.oninput;
        this.oninvalid = config.oninvalid;
        this.onkeydown = config.onkeydown;
        this.onkeypress = config.onkeypress;
        this.onkeyup = config.onkeyup;
        this.onload = config.onload;
        this.onloadeddata = config.onloadeddata;
        this.onloadedmetadata = config.onloadedmetadata;
        this.onloadstart = config.onloadstart;
        this.onmousedown = config.onmousedown;
        this.onmousemove = config.onmousemove;
        this.onmouseout = config.onmouseout;
        this.onmouseover = config.onmouseover;
        this.onmouseup = config.onmouseup;
        this.onmousewheel = config.onmousewheel;
        this.onoffline = config.onoffline;
        this.ononline = config.ononline;
        this.onpagehide = config.onpagehide;
        this.onpageshow = config.onpageshow;
        this.onpaste = config.onpaste;
        this.onpause = config.onpause;
        this.onplay = config.onplay;
        this.onplaying = config.onplaying;
        this.onpopstate = config.onpopstate;
        this.onprogress = config.onprogress;
        this.onratechange = config.onratechange;
        this.onreset = config.onreset;
        this.onresize = config.onresize;
        this.onscroll = config.onscroll;
        this.onsearch = config.onsearch;
        this.onseeked = config.onseeked;
        this.onseeking = config.onseeking;
        this.onselect = config.onselect;
        this.onstalled = config.onstalled;
        this.onstorage = config.onstorage;
        this.onsubmit = config.onsubmit;
        this.onsuspend = config.onsuspend;
        this.ontimeupdate = config.ontimeupdate;
        this.ontoggle = config.ontoggle;
        this.onunload = config.onunload;
        this.onvolumechange = config.onvolumechange;
        this.onwaiting = config.onwaiting;
        this.onwheel = config.onwheel;
        this.open = config.open;
        this.optimum = config.optimum;
        this.pattern = config.pattern;
        this.placeholder = config.placeholder;
        this.poster = config.poster;
        this.preload = config.preload;
        this.readonly = config.readonly;
        this.required = config.required;
        this.reversed = config.reversed;
        this.rows = config.rows;
        this.rowspan = config.rowspan;
        this.sandbox = config.sandbox;
        this.scope = config.scope;
        this.selected = config.selected;
        this.shape = config.shape;
        this.size = config.size;
        this.sizes = config.sizes;
        this.span = config.span;
        this.spellcheck = config.spellcheck;
        this.srcdoc = config.srcdoc;
        this.srclang = config.srclang;
        this.srcset = config.srcset;
        this.start = config.start;
        this.step = config.step;
        this.style = config.style;
        this.tabindex = config.tabindex;
        this.target =config.target;
        this.title = config.title;
        this.translate = config.translate;
        this.type = config.type;
        this.usemap = config.usemap;
        this.value = config.value;
        this.width = config.width;
        this.wrap = config.wrap;

        this.customAttr = config.customAttr;
        this.customAttrName = config.customAttrName;
        this.customAttrValue = config.customAttrValue;
    }

    init(container) {
        let element = document.createElement(this.element);
        if(this.id) {
        element.id = this.id;
        }

        if(this.class) {
            let listOfClass = this.class;
            if(listOfClass.includes(" ")) {
                let classes =this.class.split(" ");
                for (let piece of classes) {
                   element.classList.add(piece); 
                }
            }else {
                element.classList.add(this.class);
            }
        }

        if(this.innerText) {
        element.innerText = this.innerText;
        }

        if(this.innerHTML) {
        element.innerHTML = this.innerHTML;
        }

        if(this.type) {
        element.type = this.type;
        }

        if(this.href) {
        element.href = this.href;
        }

        if(this.src) {
        element.src = this.src;
        }

        if(this.rel) {
        element.rel = this.rel;
        }

        if(this.accept) {
            element.setAttribute("accept", this.accept);
        }

        if(this.acceptCharset) {
            element.setAttribute("accept-charset", this.acceptCharset);
        }

        if(this.accesskey) {
            element.setAttribute("accesskey", this.accesskey);
        }

        if(this.action) {
            element.setAttribute("action", this.action);
        }

        if(this.alt) {
            element.setAttribute("alt", this.alt);
        }

        if(this.async) {
            element.setAttribute("async", this.async);
        }

        if(this.autocomplete) {
            element.setAttribute("autocomplete", this.autocomplete);
        }

        if(this.autofocus) {
            element.setAttribute("autofocus", this.autofocus);
        }

        if(this.charset) {
            element.setAttribute("charset", this.charset);
        }

        if(this.checked) {
            element.setAttribute("checked", this.checked);
        }

        if(this.cite) {
            element.setAttribute("cite", this.cite);
        }

        if(this.cols) {
            element.setAttribute("cols", this.cols);
        }

        if(this.colspan) {
            element.setAttribute("colspan", this.colspan);
        }

        if(this.content) {
            element.setAttribute("content", this.content);
        }

        if(this.contenteditable) {
            element.setAttribute("contenteditable", this.contenteditable);
        }

        if(this.controls) {
            element.setAttribute("controls", this.controls);
        }

        if(this.coords) {
            element.setAttribute("coords", this.coords);
        }

        if(this.data) {
            element.setAttribute("data", this.data);
        }

        if(this.datetime) {
            element.setAttribute("datetime", this.datetime);
        }

        if(this.default) {
            element.setAttribute("default", this.default);
        }

        if(this.defer) {
            element.setAttribute("defer", this.defer);
        }

        if(this.dir) {
            element.setAttribute("dir", this.dir);
        }

        if(this.dirName) {
            element.setAttribute("dirName", this.dirName);
        }

        if(this.disabled) {
            element.setAttribute("disabled", this.disabled);
        }

        if(this.download) {
            element.setAttribute("download", this.download);
        }

        if(this.draggable) {
            element.setAttribute("draggable", this.draggable);
        }

        if(this.enctype) {
            element.setAttribute("enctype", this.enctype);
        }

        if(this.for) {
            element.setAttribute("for", this.for);
        }

        if(this.form) {
            element.setAttribute("form", this.form);
        }

        if(this.formaction) {
            element.setAttribute("formaction", this.formaction);
        }

        if(this.headers) {
            element.setAttribute("headers", this.headers);
        }

        if(this.height) {
            element.setAttribute("height", this.height);
        }

        if(this.hidden) {
            element.setAttribute("hidden", this.hidden);
        }

        if(this.high) {
            element.setAttribute("high", this.high);
        }

        if(this.hreflang) {
            element.setAttribute("hreflang", this.hreflang);
        }

        if(this.httpEquiv) {
            element.setAttribute("http-equiv", this.httpEquiv);
        }

        if(this.ismap) {
            element.setAttribute("ismap", this.ismap);
        }

        if(this.kind) {
            element.setAttribute("kind", this.kind);
        }

        if(this.label) {
            element.setAttribute("label", this.label);
        }

        if(this.lang) {
            element.setAttribute("lang", this.lang);
        }

        if(this.list) {
            element.setAttribute("list", this.list);
        }

        if(this.loop) {
            element.setAttribute("loop", this.loop);
        }

        if(this.low) {
            element.setAttribute("low", this.low);
        }

        if(this.max) {
            element.setAttribute("max", this.max);
        }

        if(this.maxlength) {
            element.setAttribute("maxlength", this.maxlength);
        }

        if(this.media) {
            element.setAttribute("media", this.media);
        }

        if(this.method) {
            element.setAttribute("method", this.method);
        }

        if(this.min) {
            element.setAttribute("min", this.min);
        }

        if(this.multiple) {
            element.setAttribute("multiple", this.multiple);
        }

        if(this.muted) {
            element.setAttribute("muted", this.muted);
        }

        if(this.name) {
            element.setAttribute("name", this.name);
        }

        if(this.novalidate) {
            element.setAttribute("novalidate", this.novalidate);
        }

        if(this.onabort) {
            element.setAttribute("onabort", this.onabort);
        }

        if(this.onafterprint) {
            element.setAttribute("onafterprint", this.onafterprint);
        }

        if(this.onbeforeprint) {
            element.setAttribute("onbeforeprint", this.onbeforeprint);
        }

        if(this.onbeforeunload) {
            element.setAttribute("onbeforeunload", this.onbeforeunload);
        }

        if(this.onblur) {
            element.setAttribute("onblur", this.onblur);
        }

        if(this.oncanplay) {
            element.setAttribute("oncanplay", this.oncanplay);
        }

        if(this.oncanplaythrough) {
            element.setAttribute("oncanplaythrough", this.oncanplaythrough);
        }

        if(this.onchange) {
            element.setAttribute("onchange", this.onchange);
        }

        if(this.onclick) {
            element.setAttribute("onclick", this.onclick);
        }

        if(this.oncontextmenu) {
            element.setAttribute("oncontextmenu", this.oncontextmenu);
        }

        if(this.oncopy) {
            element.setAttribute("oncopy", this.oncopy);
        }

        if(this.oncuechange) {
            element.setAttribute("oncuechange", this.oncuechange);
        }

        if(this.oncut) {
            element.setAttribute("oncut", this.oncut);
        }

        if(this.ondblclick) {
            element.setAttribute("ondblclick", this.ondblclick);
        }

        if(this.ondrag) {
            element.setAttribute("ondrag", this.ondrag);
        }

        if(this.ondragend) {
            element.setAttribute("ondragend", this.ondragend);
        }

        if(this.ondragenter) {
            element.setAttribute("ondragenter", this.ondragenter);
        }

        if(this.ondragleave) {
            element.setAttribute("ondragleave", this.ondragleave);
        }

        if(this.ondragover) {
            element.setAttribute("ondragover", this.ondragover);
        }

        if(this.ondragstart) {
            element.setAttribute("ondragstart", this.ondragstart);
        }

        if(this.ondrop) {
            element.setAttribute("ondrop", this.ondrop);
        }

        if(this.ondurationchange) {
            element.setAttribute("ondurationchange", this.ondurationchange);
        }

        if(this.onemptied) {
            element.setAttribute("onemptied", this.onemptied);
        }

        if(this.onended) {
            element.setAttribute("onended", this.onended);
        }

        if(this.onerror) {
            element.setAttribute("onerror", this.onerror);
        }

        if(this.onfocus) {
            element.setAttribute("onfocus", this.onfocus);
        }

        if(this.onhashchange) {
            element.setAttribute("onhashchange", this.onhashchange);
        }

        if(this.oninput) {
            element.setAttribute("oninput", this.oninput);
        }

        if(this.oninvalid) {
            element.setAttribute("oninvalid", this.oninvalid);
        }

        if(this.onkeydown) {
            element.setAttribute("onkeydown", this.onkeydown);
        }

        if(this.onkeypress) {
            element.setAttribute("onkeypress", this.onkeypress);
        }

        if(this.onkeyup) {
            element.setAttribute("onkeyup", this.onkeyup);
        }

        if(this.onload) {
            element.setAttribute("onload", this.onload);
        }

        if(this.onloadeddata) {
            element.setAttribute("onloadeddata", this.onloadeddata);
        }

        if(this.onloadedmetadata) {
            element.setAttribute("onloadedmetadata", this.onloadedmetadata);
        }

        if(this.onloadstart) {
            element.setAttribute("onloadstart", this.onloadstart);
        }

        if(this.onmousedown) {
            element.setAttribute("onmousedown", this.onmousedown);
        }

        if(this.onmousemove) {
            element.setAttribute("onmousemove", this.onmousemove);
        }

        if(this.onmouseout) {
            element.setAttribute("onmouseout", this.onmouseout);
        }

        if(this.onmouseover) {
            element.setAttribute("onmouseover", this.onmouseover);
        }

        if(this.onmouseup) {
            element.setAttribute("onmouseup", this.onmouseup);
        }

        if(this.onmousewheel) {
            element.setAttribute("onmousewheel", this.onmousewheel);
        }

        if(this.onoffline) {
            element.setAttribute("onoffline", this.onoffline);
        }

        if(this.ononline) {
            element.setAttribute("ononline", this.ononline);
        }

        if(this.onpagehide) {
            element.setAttribute("onpagehide", this.onpagehide);
        }

        if(this.onpageshow) {
            element.setAttribute("onpageshow", this.onpageshow);
        }

        if(this.onpaste) {
            element.setAttribute("onpaste", this.onpaste);
        }

        if(this.onpause) {
            element.setAttribute("onpause", this.onpause);
        }

        if(this.onplay) {
            element.setAttribute("onplay", this.onplay);
        }

        if(this.onplaying) {
            element.setAttribute("onplaying", this.onplaying);
        }

        if(this.onpopstate) {
            element.setAttribute("onpopstate", this.onpopstate);
        }

        if(this.onprogress) {
            element.setAttribute("onprogress", this.onprogress);
        }

        if(this.onratechange) {
            element.setAttribute("onratechange", this.onratechange);
        }

        if(this.onreset) {
            element.setAttribute("onreset", this.onreset);
        }

        if(this.onresize) {
            element.setAttribute("onresize", this.onresize);
        }

        if(this.onscroll) {
            element.setAttribute("onscroll", this.onscroll);
        }

        if(this.onsearch) {
            element.setAttribute("onsearch", this.onsearch);
        }

        if(this.onseeked) {
            element.setAttribute("onseeked", this.onseeked);
        }

        if(this.onseeking) {
            element.setAttribute("onseeking", this.onseeking);
        }

        if(this.onselect) {
            element.setAttribute("onselect", this.onselect);
        }

        if(this.onstalled) {
            element.setAttribute("onstalled", this.onstalled);
        }

        if(this.onstorage) {
            element.setAttribute("onstorage", this.onstorage);
        }

        if(this.onsubmit) {
            element.setAttribute("onsubmit", this.onsubmit);
        }

        if(this.onsuspend) {
            element.setAttribute("onsuspend", this.onsuspend);
        }

        if(this.ontimeupdate) {
            element.setAttribute("ontimeupdate", this.ontimeupdate);
        }

        if(this.ontoggle) {
            element.setAttribute("ontoggle", this.ontoggle);
        }

        if(this.onunload) {
            element.setAttribute("onunload", this.onunload);
        }

        if(this.onvolumechange) {
            element.setAttribute("onvolumechange", this.onvolumechange);
        }

        if(this.onwaiting) {
            element.setAttribute("onwaiting", this.onwaiting);
        }

        if(this.onwheel) {
            element.setAttribute("onwheel", this.onwheel);
        }

        if(this.open) {
            element.setAttribute("open", this.open);
        }

        if(this.optimum) {
            element.setAttribute("optimum", this.optimum);
        }

        if(this.pattern) {
            element.setAttribute("pattern", this.pattern);
        }

        if(this.placeholder) {
            element.setAttribute("placeholder", this.placeholder);
        }

        if(this.poster) {
            element.setAttribute("poster", this.poster);
        }

        if(this.preload) {
            element.setAttribute("preload", this.preload);
        }

        if(this.readonly) {
            element.setAttribute("readonly", this.readonly);
        }

        if(this.required) {
            element.setAttribute("required", this.required);
        }

        if(this.reversed) {
            element.setAttribute("reversed", this.reversed);
        }

        if(this.rows) {
            element.setAttribute("rows", this.rows);
        }

        if(this.rowspan) {
            element.setAttribute("rowspan", this.rowspan);
        }

        if(this.sandbox) {
            element.setAttribute("sandbox", this.sandbox);
        }

        if(this.scope) {
            element.setAttribute("scope", this.scope);
        }

        if(this.selected) {
            element.setAttribute("selected", this.selected);
        }

        if(this.shape) {
            element.setAttribute("shape", this.shape);
        }

        if(this.size) {
            element.setAttribute("size", this.size);
        }

        if(this.sizes) {
            element.setAttribute("sizes", this.sizes);
        }

        if(this.span) {
            element.setAttribute("span", this.span);
        }

        if(this.spellcheck) {
            element.setAttribute("spellcheck", this.spellcheck);
        }

        if(this.srcdoc) {
            element.setAttribute("srcdoc", this.srcdoc);
        }

        if(this.srclang) {
            element.setAttribute("srclang", this.srclang);
        }

        if(this.srcset) {
            element.setAttribute("srcset", this.srcset);
        }

        if(this.start) {
            element.setAttribute("start", this.start);
        }

        if(this.step) {
            element.setAttribute("step", this.step);
        }

        if(this.style) {
            element.setAttribute("style", this.style);
        }

        if(this.tabindex) {
            element.setAttribute("tabindex", this.tabindex);
        }

        if(this.target) {
            element.setAttribute("target", this.target);
        }

        if(this.title) {
            element.setAttribute("title", this.title);
        }

        if(this.translate) {
            element.setAttribute("translate", this.translate);
        }

        if(this.type) {
            element.setAttribute("type", this.type);
        }

        if(this.usemap) {
            element.setAttribute("usemap", this.usemap);
        }

        if(this.value) {
            element.setAttribute("value", this.value);
        }

        if(this.width) {
            element.setAttribute("width", this.width);
        }

        if(this.wrap) {
            element.setAttribute("wrap", this.wrap);
        }

        if(this.customAttr && (this.customAttr === true)) {
            element.setAttribute(this.customAttrName, this.customAttrValue);
        }

        container.appendChild(element);
    }
}